name: PR Check

on:
  pull_request:
    paths:
      - "force-app/**"

jobs:
  Hello:
    name: Say Hello
    runs-on: ubuntu-latest
    steps:
      - name: Run say hello
        run: echo "I will take test ${github.event.pull_request.body}"
